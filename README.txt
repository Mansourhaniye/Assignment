MagicList.py File :
	
	include implementation of magiclist class and main function with few valid examples,
	so you can to run the file.

Regular_Tests.py File:
	
	include some tests using pytest (without using classes) and main function,
	so you can to run the file.

clsType_Tests.py File:
	
	include some tests using pytest using Person class and main function,
	so you can to run the file.